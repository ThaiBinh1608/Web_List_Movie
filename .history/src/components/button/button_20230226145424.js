import React from 'react';

const button = ({onClick,className,children}) => {
  
    return (
        <button onClick={} 
        className=``>
        {children}
      </button>
    );
};

export default button;