import React from 'react';

const button = ({onClick,className,children}) => {
  
    return (
        <button onClick={} 
        className={}`py-3 px-6 text-2xl  rounded-lg capitalize bg-primary w-full mt-auto`}>
        {children}
      </button>
    );
};

export default button;