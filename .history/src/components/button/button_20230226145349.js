import React from 'react';

const button = ({onClick,c}) => {
  
    return (
        <button onClick={} 
        className="py-3 px-6 text-2xl  rounded-lg capitalize bg-primary w-full mt-auto">
        Xem ngay
      </button>
    );
};

export default button;